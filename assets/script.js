/*class User {
    constructor(_firstName, _lastName, _age, _location) {
        this.firstName =_firstName
        this.lastName =_lastName
        this.age =_age
        this.location =_location
    }
    isSameAge(user) {
        let age1=this.age
        let age2=user.age
        let name1=this.firstName
        let name2=user.firstName
        if (age1-age2 < 0){console.log(name2+" "+"is older then "+name1)}else if(age1-age2>0){console.log(name1+" "+"is older then "+name2)}else{console.log("they have same age")}
    }

}
const utenteA= new User("lorenzo","zuccante",27,"rome")
const utenteB= new User("paolo","rossi",32,"torino")
const utenteC= new User("francesco","virgolini",27,"monza")
utenteC.isSameAge(utenteB)*/
